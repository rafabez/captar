# Database
DB_PASSWORD=your-db-password-here

# Security (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here

# Encryption (generate with: openssl rand -hex 16)
ENCRYPTION_KEY=your-encryption-key-here

# Domain (captar.ia.br owned but not wired up — currently served on the free
# wildcard captar.srv1842127.hstgr.cloud, see docker/traefik/dynamic.yml)
DOMAIN=captar.srv1842127.hstgr.cloud
